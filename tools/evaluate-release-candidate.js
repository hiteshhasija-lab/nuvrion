import {readFile,writeFile} from 'node:fs/promises';import {evaluateReleaseCandidate,ReleaseCandidateError} from '../modules/platform/src/release-candidate.js';
const [manifestPath,reportPath]=process.argv.slice(2);if(!manifestPath)throw new Error('Usage: node tools/evaluate-release-candidate.js <candidate.json> [report.json]');const manifest=JSON.parse(await readFile(manifestPath,'utf8'));try{const report=evaluateReleaseCandidate(manifest);if(reportPath)await writeFile(reportPath,JSON.stringify(report,null,2),{mode:0o600});console.log(JSON.stringify(report));}catch(error){if(error instanceof ReleaseCandidateError){if(reportPath)await writeFile(reportPath,JSON.stringify(error.report,null,2),{mode:0o600});console.error(JSON.stringify(error.report));process.exitCode=1;}else throw error;}
