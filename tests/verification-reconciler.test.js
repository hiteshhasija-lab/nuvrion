import test from 'node:test';import assert from 'node:assert/strict';import {VerificationReconciler,VerificationReconciliationError} from '../modules/tasks/src/verification-reconciler.js';
function fixture(powerState='running'){const task={id:'task-1',status:'verification_required',operation:'start',target:{id:'resource-1',type:'virtual_machine'},providerNativeId:'vm-1',providerReference:'provider-ref',connectionId:'connection-1'},calls=[];return {calls,task,reconciler:new VerificationReconciler({store:{async get(){return task},async complete(_id,result){calls.push(['complete',result])}},connections:{async get(){return {id:'connection-1'}}},provider:{async discover(){calls.push(['discover']);return [{resourceType:'virtual_machine',nativeId:'vm-1',attributes:{powerState}}]}},inventory:{async get(){return {id:'resource-1'}},async applyOperation(...args){calls.push(['inventory',...args])}}})};}
test('reconciliation confirms observed final state without executing provider operations',async()=>{const {reconciler,calls}=fixture('running'),result=await reconciler.reconcile('task-1');assert.equal(result.result.code,'NUV_OPERATION_RECONCILED');assert.deepEqual(calls.map(x=>x[0]),['discover','inventory','complete']);});
test('state mismatch remains unresolved and never mutates task or inventory',async()=>{const {reconciler,calls}=fixture('stopped');await assert.rejects(()=>reconciler.reconcile('task-1'),error=>error instanceof VerificationReconciliationError&&error.code==='NUV_RECONCILIATION_STATE_MISMATCH');assert.deepEqual(calls,[['discover']]);});
test('only verification-required tasks can enter reconciliation',async()=>{const {reconciler,task}=fixture();task.status='failed';await assert.rejects(()=>reconciler.reconcile('task-1'),error=>error.code==='NUV_TASK_NOT_RECONCILABLE');});
