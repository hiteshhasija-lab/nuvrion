import test from 'node:test';import assert from 'node:assert/strict';import {scryptSync} from 'node:crypto';import {hashPassword,passwordMatches,needsPasswordRehash} from '../modules/identity/src/password-hasher.js';
test('new passwords use calibrated Argon2id and verify safely',()=>{const encoded=hashPassword('A strong password!');assert.match(encoded,/^argon2id\$19456\$2\$1\$/);assert.equal(passwordMatches('A strong password!',encoded),true);assert.equal(passwordMatches('wrong',encoded),false);assert.equal(needsPasswordRehash(encoded),false);});
test('legacy scrypt hashes verify and request transparent rehash',()=>{const salt=Buffer.alloc(16,7),encoded=`scrypt$16384$8$1$${salt.toString('base64url')}$${scryptSync('legacy password',salt,32,{N:16384,r:8,p:1}).toString('base64url')}`;assert.equal(passwordMatches('legacy password',encoded),true);assert.equal(needsPasswordRehash(encoded),true);assert.equal(passwordMatches('wrong',encoded),false);});
