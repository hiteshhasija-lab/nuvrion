import test from 'node:test'; import assert from 'node:assert/strict'; import { TaskStore } from '../modules/tasks/src/task-store.js';
test('idempotency returns original task',()=>{const s=new TaskStore(); const a=s.create({operation:'start',targetId:'vm-1',correlationId:'c',idempotencyKey:'same'}); const b=s.create({operation:'start',targetId:'vm-1',correlationId:'c',idempotencyKey:'same'}); assert.equal(a.created,true); assert.equal(b.created,false); assert.equal(a.task.id,b.task.id);});
test('task progresses to completion',()=>{const s=new TaskStore(); const {task}=s.create({operation:'start',targetId:'vm-1',correlationId:'c',idempotencyKey:'k'}); assert.equal(s.claim().status,'running'); s.complete(task.id,{observedFinalState:'running'}); assert.equal(s.get(task.id).status,'completed');});
